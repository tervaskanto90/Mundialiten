/// <reference types="vite/client" />

// Inyectado por Vite (define) — true sólo en el build de la rama `staging`.
declare const __STAGING__: boolean
// Versión (package.json) y hash del commit del build, inyectados por Vite.
declare const __APP_VERSION__: string
declare const __BUILD_SHA__: string
